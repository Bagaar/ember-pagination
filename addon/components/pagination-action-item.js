import Component from '@ember/component';
import layout from '../templates/components/pagination-action-item';

export default Component.extend({
  /**
   * Arguments
   */

  isDisabled: null,

  /**
   * State
   */

  layout,
  tagName: '',
});
